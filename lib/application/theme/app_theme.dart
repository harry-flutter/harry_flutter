import 'package:flutter/material.dart';

class AppTheme {
  static ThemeData lightTheme() {
    return ThemeData(
      fontFamily: 'HpRegularFont',
      textTheme: const TextTheme(
          // bodyText2: TextStyle(fontSize: 15),
          ),
    );
  }
}
