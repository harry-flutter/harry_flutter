import 'package:flutter/material.dart';

class WizardsPage extends StatelessWidget {
  const WizardsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Wizards Page'),
    );
  }
}
