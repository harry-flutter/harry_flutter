import 'package:flutter/material.dart';

class ElixirsPage extends StatelessWidget {
  const ElixirsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Elixirs'));
  }
}
